---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: orphanagepolicies.orphanage.kponos.io
spec:
  group: orphanage.kponos.io
  names:
    kind: OrphanagePolicy
    listKind: OrphanagePolicyList
    plural: orphanagepolicies
    singular: orphanagepolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OrphanagePolicy is the Schema for the orphanagepolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OrphanagePolicySpec defines the desired state of OrphanagePolicy.
            properties:
              resourceTypes:
                description: |-
                  ResourceTypes specifies the Kubernetes resource types to monitor
                  Supported values: "Secret", "ConfigMap"
                items:
                  description: ResourceType represents a Kubernetes resource type
                    to monitor
                  enum:
                  - Secret
                  - ConfigMap
                  type: string
                type: array
            type: object
          status:
            description: OrphanagePolicyStatus defines the observed state of OrphanagePolicy.
            properties:
              lastChanged:
                description: LastChanged is the timestamp when the status was last
                  updated
                format: date-time
                type: string
              orphanCount:
                description: OrphanCount is the total number of orphaned resources
                type: integer
              orphans:
                description: Orphans is the list of orphaned resources
                items:
                  description: Orphan represents an orphaned resource
                  properties:
                    kind:
                      description: Kind is the Kubernetes resource kind (e.g., "Secret",
                        "ConfigMap")
                      type: string
                    name:
                      description: Name is the name of the orphaned resource
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
